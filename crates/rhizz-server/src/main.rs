//! Entry point for the `rhizz-server` binary.

fn main() {
    // Bootstrap placeholder: the axum-based HTTP server lands in the next
    // incremental step, keeping this commit green and compilable.
    println!("rhizz-server: bootstrap crate");
}